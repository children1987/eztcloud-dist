# 请保留这一行注释，以避免utf-8 BOM问题
server {
    listen 8084 default_server;

    root /workspace/isw-front-mobile;
    index index.html;

    location = / {
        try_files /index.html =404;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
